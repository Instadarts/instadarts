plans
tmp
node_modules
.vscode
test-results
playwright-report

# Build output. Not committed — a release is built by CI and attached to the GitHub release, so
# nobody has to build this and the repository never carries 43MB of WASM per revision.
dist
release

# A deployment's own settings, under either accepted name. The committed file is
# instadarts.config.example.jsonc.
instadarts.config.jsonc
instadarts.config.json
