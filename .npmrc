# Refuse to install on a Node older than package.json's `engines` says, rather than warning and
# carrying on. Without this the first sign of a too-old Node is a syntax error from inside a
# dependency, which is an unhelpful thing to hand somebody who has just downloaded this.
engine-strict=true
